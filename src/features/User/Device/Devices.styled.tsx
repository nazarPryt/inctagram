import styled from 'styled-components'

export const DevicesStyled = styled.div`
    display: flex;
    flex-direction: column;
    padding: 20px;
    gap: 15px;

    .terminateAllSession {
        align-self: flex-end;
    }
    .allSessionsBox {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }
`
