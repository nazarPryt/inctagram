import styled from 'styled-components'

export const StripeWrapper = styled.button`
    cursor: pointer;
    margin-left: 50px;
    padding: 0;
    border: none;
    font: inherit;
    color: inherit;
    background-color: transparent;
`
