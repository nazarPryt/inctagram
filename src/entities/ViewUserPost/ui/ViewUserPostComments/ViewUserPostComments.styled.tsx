import styled from 'styled-components'

export const ViewUserPostCommentsWrapper = styled.ul`
  flex: 1;

  overflow-y: scroll;

  &::-webkit-scrollbar {
    width: 0;
  }

  &::-webkit-scrollbar {
    width: 0;
  }
`
